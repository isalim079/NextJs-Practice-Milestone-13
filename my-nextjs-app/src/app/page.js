import Counter from "@/components/Counter/Counter";
import React from "react";

const page = () => {
    return (
        <div>
            <h1>Have a look on you (home page)</h1>
            <Counter />
        </div>
    );
};

export default page;
