
// you can use metadata. It's title will be in browser tab like Helmet
export const metadata = {
    title: "About Page",
    description: "This is about page",
};
const AboutPage = () => {
    return (
        <div>
            <h1>About page with header footer</h1>
        </div>
    );
};

export default AboutPage;
